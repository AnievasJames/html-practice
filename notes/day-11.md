Day-11

I learned about:

shadows

margins = it's a space around an element.

float= allows other elements to float around it

overflow= property that sets the desired

                    behavior when content does not
                    fit in the parent element box (overlflows)
